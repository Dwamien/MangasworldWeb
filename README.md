# MangasworldWeb

1) utiliser la commande composer update pour créer le vendor

2) utiliser la commande copy .env.example .env pour générer le fichier .env 

3) utiliser la commande php artisan key:generate pour créer la clé de cryptage APP_KEY dans le fichier .env
